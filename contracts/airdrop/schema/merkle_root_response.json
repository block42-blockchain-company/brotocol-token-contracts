{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MerkleRootResponse",
  "description": "This structure describes the fields for merkle root response message.",
  "type": "object",
  "required": [
    "merkle_root",
    "stage"
  ],
  "properties": {
    "merkle_root": {
      "description": "merkle root string represented as hash",
      "type": "string"
    },
    "stage": {
      "description": "airdrop stage",
      "type": "integer",
      "format": "uint8",
      "minimum": 0.0
    }
  }
}
